<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- AICore declares minSdk 31, but we support 26+.
         Runtime guard in LlmOrchestrator.isAiCoreAvailable() ensures AICore
         is only used on Android 14+ (SDK 34+) devices. -->
    <uses-sdk tools:overrideLibrary="com.google.ai.edge.aicore" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- BLE advertising — sole discovery channel. The phone advertises its
         LAN endpoint (IPv4 + port) so the TV can reach the HTTP server
         directly, even on networks that would block mDNS (AP/client
         isolation, VLAN-segmented mesh Wi-Fi, aggressive multicast
         filtering). Degrades gracefully when Bluetooth is off or the
         permission is denied — the phone simply becomes undiscoverable. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <!-- Background work (model updates, token refresh) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- DATA_SYNC: WorkManager's SystemForegroundService runs ModelDownloadWorker
         as a time-bounded dataSync FGS; keep this permission for that worker.
         CONNECTED_DEVICE: CompanionService maintains long-lived P2P connectivity
         with the paired TV (BLE advert + Ktor HTTP server) and must use the
         connectedDevice type on Android 15/16 to avoid the dataSync 6 h/24 h
         quota that was killing the service with ForegroundServiceDidNotStop-
         InTimeException (#459). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Play Store: phone app — NOT a TV app. Touchscreen not strictly required
         (the app also works with keyboard/accessibility navigation), so mark
         required=false to widen device availability without affecting the
         non-Leanback category filtering on Play Store. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".WatchBuddyPhoneApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WatchBuddy">

        <!-- Main launcher activity -->
        <activity
            android:name=".phone.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WatchBuddy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- NOT android.intent.category.LEANBACK_LAUNCHER — that's the TV APK -->
            </intent-filter>
        </activity>

        <!-- Disable the default WorkManager auto-initializer so Hilt can inject
             HiltWorkerFactory before WorkManager starts. The meta-data entry
             being removed is declared by the androidx.work:work-runtime-ktx
             library manifest (via androidx.startup) and merged here at build
             time; the tools:node="remove" directive prevents the startup
             library from auto-starting WorkManager with the default factory. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Diagnostic report sharing. Exposes filesDir/diagnostics/ so crash
             reports can be attached to an ACTION_SEND intent from the Home
             screen banner. See CrashReporter / DiagnosticShare. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.diagnostics.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- Companion HTTP server (BLE advertiser + Ktor). Declared as
             `connectedDevice` — not `dataSync` — because the service maintains
             long-lived P2P connectivity with the paired TV. Android 15/16 time-
             box `dataSync` FGS at ~6 h/24 h which was crashing the service with
             ForegroundServiceDidNotStopInTimeException (#459). -->
        <service
            android:name=".service.CompanionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- WorkManager foreground service — required for long-running downloads on Android 10+.
             Merges with WorkManager's own SystemForegroundService manifest entry to declare
             the dataSync foreground service type used by ModelDownloadWorker. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>
</manifest>
