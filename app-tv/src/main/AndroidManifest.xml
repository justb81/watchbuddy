<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- BLE scanning — secondary discovery channel used when mDNS is blocked
         by the router (AP/client isolation, VLAN-segmented mesh Wi-Fi,
         aggressive multicast filtering). The TV scans for phones that
         advertise their LAN endpoint over BLE and falls through to plain
         HTTP. `neverForLocation` opts out of the location runtime prompt
         since we never derive physical location from scan results.
         Degrades gracefully when Bluetooth is off or the permission is
         denied — NSD keeps working. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Boot autostart — BootReceiver fires on BOOT_COMPLETED when the user
         has enabled "Autostart at TV boot" in Settings; it spawns
         TvDiscoveryService so discovery keeps running even when the launcher
         activity hasn't been opened yet. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Play Store: TV app — no touchscreen, leanback required for correct
         multi-APK device targeting (prevents phone devices from receiving the
         TV AAB which has a higher versionCode). -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="true" />

    <!-- Declare TV hardware support without requiring it — the leanback
         requirement above is the primary gate for Play Store device targeting. -->
    <uses-feature android:name="android.hardware.type.television" android:required="false" />

    <!-- Hardware features unavailable on TV — declared as not required to
         prevent implicit feature requirements from library dependencies. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <!-- BLE receiver role — required for PhoneBleScanner fallback discovery,
         but declared optional so TVs without BLE still get the APK and fall
         back to NSD alone. -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <application
        android:name=".WatchBuddyTvApp"
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.WatchBuddy">

        <!-- TV Leanback launcher -->
        <activity
            android:name=".tv.ui.TvMainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|navigation"
            android:theme="@style/Theme.WatchBuddy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- LEANBACK_LAUNCHER → Play Store shows this in the TV tab -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- TV app does not use WorkManager; disable its auto-init so the
             startup path does not build a Room-backed WorkDatabase for
             nothing (which can crash under R8 if any keep rule is off). -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Background phone discovery service. Runs NSD + BLE scanning when
             "Autostart at TV boot" is enabled so new phones are picked up
             without the user opening the launcher first. Uses `dataSync`
             foreground service type to match the phone-side CompanionService. -->
        <service
            android:name=".tv.discovery.TvDiscoveryService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- BOOT_COMPLETED receiver — gated on `isAutostartEnabled` preference.
             Must be exported=true: system broadcasts originate from the
             platform and Android 12+ rejects exported=false receivers for
             implicit system actions. -->
        <receiver
            android:name=".tv.boot.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
