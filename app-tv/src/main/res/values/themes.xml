<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        TV-App verwendet Compose for TV → benötigt kein AppCompat-Theme.
        Theme.DeviceDefault.NoActionBar ist auf allen Android TV / Google TV
        Geräten (API 26+) zuverlässig verfügbar.
    -->
    <style name="Theme.WatchBuddy" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowOverscan">false</item>
    </style>
</resources>
